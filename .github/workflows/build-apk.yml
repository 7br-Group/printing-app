# Android APK build — cancelled in favour of web-only Flask app
name: (Disabled) Build Android APK
on:
  workflow_dispatch:
jobs:
  build:
    runs-on: ubuntu-22.04
    steps:
      - run: echo "Android build removed — project is web-only"
